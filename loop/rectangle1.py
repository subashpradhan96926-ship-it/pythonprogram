class rectangle:
	def __init__self(self,length,width):
		self.length = length
		self;width = width
	def area(self):
		return self.length * self.width
r = rectangle(3, 5)
print("area of rectangle:", r.area())
